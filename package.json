{
  "name": "diepio-online-tank-arena",
  "version": "1.0.0",
  "description": "Authoritative WebSocket tank arena inspired by diep.io, with bots, upgrades, and a large map.",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "test:all": "npm test && npm run test:browser"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
